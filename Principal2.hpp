#include "..\include\Principal.hpp"

Jungle::Principal::Principal();
window(sf;;VideoMode(800.0f, 600.0f), "Jungle++")
{
    executar();
}
void Jungle::Principal::executar(){
    while(true){
        
    }
}